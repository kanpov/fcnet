## fcnet

- `fcnet-core`: Core type definitions shared between all other crates.
- `fcnet-embedded`: An implementation that is embedded directly into your application's process and can be called directly.
- `fcnet-cli`: An implementation that runs ``
